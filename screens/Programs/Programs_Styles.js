import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  programContainer: {
    flexDirection: "row",
    flexWrap: "wrap",
    gap: 2,
    justifyContent: "center",
    alignContent: "center",
  },
  pageContainer: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
});
